# Ukraine Conflict Lakehouse — Python dependencies
# Verified working together on Python 3.12 (Apple Silicon wheels available).
deltalake>=1.6
duckdb>=1.5
pandas>=2.2
pyarrow>=14        # required by delta-rs for the pandas <-> Arrow bridge
shapely>=2.0
numpy>=1.26
requests>=2.31
pyyaml>=6.0
python-dotenv>=1.0

# Add later, when you get to silver/gold:
# polars           # faster transforms if pandas gets slow
# duckdb spatial extension is installed at runtime, not via pip
